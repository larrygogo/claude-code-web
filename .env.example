# ===========================================
# Claude Web - 环境变量配置
# ===========================================
# 开发环境无需任何 .env 文件，所有配置都有默认值。
# JWT 密钥在生产环境首次启动时自动生成并持久化。
# API Key、模型配置、功能开关等通过管理后台配置。
# ===========================================

# 以下为可选配置（均有默认值）
# NODE_ENV=development
# DATABASE_URL="file:./data/db/claude-web.db"
# DATA_DIR=./data/claude-web

# 端口配置
# PORT=3001                  # 服务端端口
# CLIENT_PORT=3000           # 客户端端口
# VITE_SERVER_PORT=3001      # 客户端连接服务端的端口（与 PORT 保持一致）
