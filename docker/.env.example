# ===========================================
# Claude Web Docker - 环境变量配置
# ===========================================
# JWT 密钥在首次启动时自动生成并持久化。
# API Key、模型配置、功能开关等通过管理后台配置。
# ===========================================

# 以下为可选配置（均有默认值）
# NODE_ENV=production
# DATA_DIR=./data/claude-web

# 端口配置
# CLIENT_PORT=3000
# SERVER_PORT=3001

# 跨域配置（当使用自定义域名或端口时需设置）
# CLIENT_URL=http://localhost:3000
# VITE_API_URL=http://localhost:3001
